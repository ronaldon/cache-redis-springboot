spring:
  cache:
    type: redis
    redis:
      time-to-live: 60000
      cache-null-values: false
  redis:
      host: localhost
      port: 6379

  datasource.url: jdbc:h2:mem:db
  jpa.show-sql: true

logging:
  level:
    br.com.projectname: DEBUG
    org.springframework.web: DEBUG
